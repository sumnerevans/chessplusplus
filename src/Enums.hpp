#pragma once

enum BoardOrientation {
    WhiteUp,
    BlackUp,
};

enum PieceType {
    Pawn,
    Knight,
    Bishop,
    Rook,
    Queen,
    King,
};

enum PlayerColour {
    Black,
    White,
};

enum SquareColour {
    Dark,
    Light,
    Selected,
};